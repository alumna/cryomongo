name: Specs

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  tests:
    strategy:
      fail-fast: false
      matrix:
        include:
          - topology: standalone
            uri: mongodb://bob:pwd123@localhost:27017/?authSource=admin&compressors=zlib,snappy,zstd
          - topology: replicaset
            uri: mongodb://bob:pwd123@127.0.0.1:27017/?replicaSet=rs0&authSource=admin&compressors=zlib,snappy,zstd
          - topology: sharded
            uri: mongodb://bob:pwd123@localhost:27017,localhost:27016/?authSource=admin&compressors=zlib,snappy,zstd
          - topology: load-balanced
            uri: mongodb://bob:pwd123@127.0.0.1:8000/?loadBalanced=true&authSource=admin&compressors=zlib,snappy,zstd
    runs-on: ubuntu-24.04
    name: MongoDB 8.0 ${{ matrix.topology }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Start MongoDB ${{ matrix.topology }} (8.0)
        run: scripts/docker-topology.sh ${{ matrix.topology }}
      - name: Install HAProxy (load-balanced)
        if: matrix.topology == 'load-balanced'
        run: |
          set -euo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y haproxy
          spec/support/run-load-balancer.sh start
          cat tmp/lb-uri.env

      - name: Install Crystal
        uses: crystal-lang/install-crystal@v1
        with:
          crystal: latest

      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y libzstd-dev
          shards install

      - name: Run Specs
        env:
          TOPOLOGY: ${{ matrix.topology }}
          MONGODB_URI: ${{ matrix.uri }}
          SINGLE_MONGOS_LB_URI: mongodb://bob:pwd123@127.0.0.1:8000/?loadBalanced=true&authSource=admin&compressors=zlib,snappy,zstd
          MULTI_MONGOS_LB_URI: mongodb://bob:pwd123@127.0.0.1:8001/?loadBalanced=true&authSource=admin&compressors=zlib,snappy,zstd
          UTF_TIMING_LOG: tmp/utf-timing.log
          CRYSTAL_WORKERS: "2"
          UTF_RUN_TWO_MONGOS: "1"
        run: |
          mkdir -p tmp
          crystal spec -Dpreview_mt -Dexecution_context

      - name: Upload UTF timing log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: utf-timing-${{ matrix.topology }}
          path: tmp/utf-timing.log
          if-no-files-found: ignore
