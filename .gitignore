# /docs/
/lib/
/bin/
/.shards/
*.dwarf
.vscode
test*.cr
test*.sh

# Libraries don't need dependency lock
# Dependencies will be locked in applications that use them
/shard.lock

/data
/tmp/
/vendor/libmongocrypt/
/specifications*
drivers-evergreen-tools/
/local

# Temporary
FIXES.md
LOCAL_TESTING.md

# Local workbench only. GitHub shards install uses shard.yml (bson.cr master).
/shard.override.yml

